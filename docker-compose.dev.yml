version: "3.8"

services:
  app:
    container_name: sait-back-app
    image: sait-back-app
    build:
      context: .
      dockerfile: ./docker/develop/Dockerfile
      target: development
    volumes:
      - ./src:/app/src
    ports:
      - 4000:4000